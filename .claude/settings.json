{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [ -n \"$f\" ] && [ \"${f##*.}\" = \"py\" ] && \"${CLAUDE_PROJECT_DIR:-.}/.venv/bin/ruff\" format --quiet \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
