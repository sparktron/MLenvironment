# Fixes Implemented — 2026-04-22

This document records incremental repository fixes completed after the code audit.

## Scope and order
1. Lint hygiene easy wins (unused imports removed).
2. Morphology-search checkpoint path contract fix (`.zip` normalization).
3. Config validation hardening for core training fields.
4. GUI schema update to expose `training.device`.
5. Repo hygiene/docs updates (`.gitignore`, dependency table alignment in README).

## Validation run after each commit
- `ruff check src tests`
- `pytest -q` (or focused test module + full suite)

## Files changed
- `src/rl_framework/training/morphology_search.py`
- `src/rl_framework/utils/config.py`
- `src/rl_framework/gui/app.py`
- `tests/test_morphology_search.py`
- `tests/test_config_validation.py`
- `tests/test_gui_api.py`
- `README.md`
- `.gitignore`

## Outcomes
- Lint checks now pass for repository source and tests.
- Test suite increased from 51 to 54 passing tests.
- GUI users can now select training device class.
- Config errors fail earlier with clearer validation messages.
